// import createTuner from "@pedroloch/tuner";
// const tuner = createTuner();

const Tuner = (props) => {
	var something = "something";

	//   const startTuner = () => {
	//     tuner.start();
	//   };

	//   const stopTuner = () => {
	//     tuner.stop();
	//   };

	return (
		<>
			<p>{something}</p>

			{/* <p className="" onClick={startTuner}>
        Start Recording
      </p>

      <p className="" onClick={stopTuner}>
        Stop Recordig
      </p> */}
		</>
	);
};

export default Tuner;
